- shell:
    - [mkdir -p ~/.config, Creating config folder]

- link:
    ~/.config/i3: config/i3/
    ~/.mplayer: mplayer
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc
    ~/.xinitrc: xinitrc
