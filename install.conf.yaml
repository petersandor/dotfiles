- shell:
    - [mkdir -p ~/.config, Creating config folder]

- link:
    ~/.config/i3: config/i3
    ~/.mplayer: mplayer
    ~/.aliases: aliases
    ~/.functions: functions
    ~/.bash_profile: bash_profile
    ~/.bash_logout: bash_logout
    ~/.bashrc: bashrc
    ~/.xinitrc: xinitrc
